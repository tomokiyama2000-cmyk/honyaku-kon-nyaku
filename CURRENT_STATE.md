# CURRENT_STATE.md

このファイルは「今、プロジェクトがどの段階にいるか」を常に最新の状態に保つためのものです。
迷ったらまずここを読めば、現在地が分かるようにします。

## 現在地

🎉 **VoiceBridge(ほんやくコンニャク)は、実用段階の複数ユーザー対応Webアプリとして稼働中** 🎉

「日本語で話す→認識→翻訳→自分の声のクローンで読み上げ」という基本体験に加え、
アカウント登録・履歴管理・スマホ対応・GitHub連携まで整った、本格的なアプリになっている。
音声品質は原文・翻訳文とも「おおむね問題ない」レベルに到達済み（固有名詞の稀な誤読みは許容範囲として合意済み）。

**【重要】この会話は画像枚数の上限に達したため、新しいチャットに移行します。**
新しいチャットを始める際は、このファイル（CURRENT_STATE.md）と DEVELOPMENT_LOG.md を
GitHubリポジトリ（後述）から読み込んでもらうことで、これまでの経緯を引き継げます。

### 【直前の状況】声のトーン機能を実装中、環境変数の再設定待ち

- 直近で「読み上げの声色（標準/明るい/落ち着いた）」機能を実装し、GitHubにpush済み
- ユーザーのパソコンで `git pull` 後にサーバーを起動したところ、「翻訳サービスの準備ができていません」というエラーが表示された
  - 原因は、新しいPowerShellウィンドウで環境変数（`DEEPL_API_KEY`, `ELEVENLABS_API_KEY`）が設定されていないという、以前にも何度か発生したパターンの可能性が高い
  - ユーザーに環境変数の再設定を案内したところで、この会話が画像上限に達した
- **次のチャットでまず確認すべきこと**：環境変数の再設定後、声のトーン機能（標準/明るい/落ち着いた）が実機で正しく聞き分けられるか

## 必要な環境変数（毎回のPowerShellウィンドウで設定が必要。頻発するため永続設定を推奨）

```
$env:DEEPL_API_KEY = "（DeepLのAPIキー）"
$env:ELEVENLABS_API_KEY = "（ElevenLabsのAPIキー）"
$env:SPEECH_PROVIDER = "google"
$env:GOOGLE_SPEECH_API_KEY = "（Google Cloud Speech-to-TextのAPIキー）"
```
※実際のキーの値は、ユーザー本人が把握している（このファイルには機密情報として記載しない）

## 技術構成（2026年9月現在）

- バックエンド：Python 3.12 + Flask（`webapp/app.py`）
- データベース：SQLite（`webapp/db.py`、利用者ごとにアカウント・声のID・会話履歴を分離管理）
- 翻訳：**DeepL公式API**（`webapp/providers/translation.py`。以前は非公式なGoogle翻訳を使用していたが移行済み）
- 声のクローン：**ElevenLabs公式API**（`webapp/providers/voice.py`、Starterプラン月$5。以前は無料のXTTSを使用していたが、
  品質・商用ライセンスの問題から移行済み）
- 声のクローン以外の読み上げ：edge-tts（無料、自然な発音）
- 音声認識：ブラウザのWeb Speech API（サーバー側では処理していない）
- フロントエンド：`webapp/templates/`, `webapp/static/`（Flask + 素のHTML/CSS/JS）
- コード管理：GitHub（非公開リポジトリ `https://github.com/tomokiyama2000-cmyk/-`）。
  Claudeがコードを更新した際は都度pushし、ユーザーは`git pull`だけで最新化できる

### 設計上の重要なポイント：プロバイダー差し替え可能設計

翻訳・声のクローンは、どちらも「プロバイダー」という部品（`webapp/providers/`フォルダ）に切り出してある。
将来、料金や品質の都合で別のサービスに切り替えたくなった場合、`app.py`本体を書き換えずに、
`providers/translation.py`または`providers/voice.py`に新しいクラスを追加し、
環境変数（`TRANSLATION_PROVIDER`, `VOICE_PROVIDER`）を変えるだけで切り替えられるようにしてある。

## 実装済みの機能一覧

- 音声認識(ブラウザのWeb Speech API、自動終了/手動終了の切り替え可能)
- 翻訳(DeepL公式API、73言語)
- 声のクローン(ElevenLabs公式API、アプリ内で録音してその場で登録できる)
- 自然な声での読み上げ(edge-tts、クローンを使わない場合のデフォルト)
- テキスト入力での翻訳(音声が使えない時の代替)
- 会話履歴の保存・一覧表示・個別削除・一括削除（利用者ごとに分離）
- 原文・翻訳文それぞれの再生ボタン
- 複数ユーザー対応（アカウント登録・ログイン・ログアウト）
- 明るい配色のUI、「秘密道具」風のタイトルデザイン
- スマホからのアクセス(同じWi-Fi、またはngrok経由の外部アクセス)
- GitHub経由でのコード自動同期

## 【重要】商用サービス化についての注意点

ユーザーは将来的な本格的なサービス化（事業化）を目指しているが、実現には以下の課題が残っている（ユーザーの意思決定待ち）：

1. **法的基盤**：プライバシーポリシー・利用規約の整備（他人の声＝生体情報を扱うため）。専門家への相談を推奨
2. **同時アクセス対応**：現在はサーバー1台・単純な構成のため、多人数の同時利用には向いていない（タスクキューなどの導入が必要）
3. **ビジネスモデル**：DeepL・ElevenLabsの利用料をどう賄うか（無料提供か、課金モデルか）の検討
4. **常時稼働（クラウドデプロイ）**：Render.com等へのデプロイ準備（Dockerfile, requirements.txt）は途中まで進めていたが、
   ElevenLabsへの切り替えにより依存関係が軽量化されたため、再度デプロイ手順を見直す必要がある

## 今後の機能追加ロードマップ（優先順位順、一部実施済み）

1. テキスト入力での翻訳 → 実装済み
2. 翻訳履歴の保存（一括・個別削除） → 実装済み
3. よく使うフレーズの保存・お気に入り登録 → 未着手
4. カメラでの文字認識翻訳（OCR） → 未着手
5. オフライン対応 → 優先度低、未着手

## 次にやること

- 発展課題ロードマップの続き（お気に入り登録、カメラ翻訳など）に進む
- または、商用サービス化に向けた課題（クラウドデプロイの再開、法的基盤の検討など）に進む
- 決めたことや作業内容は DEVELOPMENT_LOG.md に追記していく
- 状況が変わったら、このファイル（CURRENT_STATE.md）を都度更新する
