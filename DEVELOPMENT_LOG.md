# DEVELOPMENT_LOG.md

開発の作業記録。新しい記録は下に追記していく。

---

## 2026-09-01

- VoiceBridgeプロジェクトの開発環境の準備を開始した
- プロジェクト用フォルダ「VoiceBridge」を作成した
- 開発の進め方を記録するため、以下の3つのファイルを整備した
  - PROJECT_PLAN.md（最終目標と開発計画）
  - DEVELOPMENT_LOG.md（このファイル：作業ログ）
  - CURRENT_STATE.md（現在の状況）
- アプリ本体のコードはまだ書いていない（次回以降のフェーズで着手予定）

### フェーズ1：土台づくり を開始

- 開発言語としてPythonを採用することを決定した（Python 3.12がインストール済みであることを確認）
- `src/hello.py` として、最小の動作確認プログラム（Hello World）を作成した
- 実行して、正常に動作することを確認した
  - 出力：「VoiceBridge: 開発環境の準備ができました！」
- これでフェーズ1「土台づくり」は完了。次はフェーズ2「音声を文字にする（音声認識）」に進む

### フェーズ2：音声を文字にする（音声認識）を開始

- システムライブラリ `portaudio19-dev` をインストールした（マイク音声を扱うために必要）
- Pythonライブラリ `SpeechRecognition`、`PyAudio` をインストールした
- `src/speech_to_text.py` を作成した
  - マイクから音声を取得し、Google音声認識サービスでテキスト化する機能
  - 日本語（ja-JP）・英語（en-US）などを指定可能
- ライブラリの読み込みは成功を確認した
- 【重要な制約】開発サーバーには物理的なマイクが無いため、実際に話して認識させるテストはここでは実施できないことを確認した（マイクデバイス一覧が空であることで確認）
  - → 実機での動作確認は、ユーザー自身のパソコンで行う必要がある

### ユーザーのWindows PCでの実機テスト（成功）

- `speech_to_text.py` をダウンロードし、`pip install SpeechRecognition` `pip install PyAudio` を実行してライブラリを導入した
- 実行時に発生したトラブルと対処
  - `speech_to_text.py` を直接コマンドとして実行しようとしてエラー → 先頭に `python` を付けて実行する必要があると解決
  - 保存先フォルダに移動せずに実行してファイルが見つからないエラー → `cd` で正しいフォルダ（Downloads）に移動して解決
  - `ModuleNotFoundError: No module named 'speech_recognition'` → ライブラリ未インストールが原因。`pip install` で解決
- `python speech_to_text.py` を実行し、マイクに向かって「こんにちは」と発話
- 結果：「認識結果: こんにちは」と正しく表示され、**音声認識の実機動作を確認できた**
- これでフェーズ2「音声を文字にする（音声認識）」は完了。次はフェーズ3「文字を翻訳する」に進む

### フェーズ3：文字を翻訳する を開始

- Pythonライブラリ `deep-translator`（Google翻訳を利用した翻訳ライブラリ）をインストールした
- `src/translate_text.py` を作成した
  - テキストを指定した言語に翻訳する機能（例：日本語→英語）
- 開発サーバー上で実行を試したところ、翻訳サービスに接続できないエラーが発生した
  - 【重要な制約】この開発サーバーは外部の翻訳サービスへのネットワーク接続が制限されているため、実際に翻訳させるテストはここでは実施できないことを確認した
  - → こちらもフェーズ2のマイクと同様、実機での動作確認はユーザー自身のパソコン（通常のインターネット接続がある環境）で行う必要がある

### ユーザーのWindows PCでの実機テスト（成功）

- `translate_text.py` をダウンロードし、`pip install deep-translator` を実行してライブラリを導入した
- 1回目の実行では翻訳サービス側の一時的な不調により失敗（`No translation was found using the current translator`）
- 2回目の実行で成功
  - 元の文章: 「こんにちは、今日はいい天気ですね。」
  - 翻訳結果: 「Hello, the weather is nice today.」
- これで**日本語→英語の翻訳が実際に動くことを確認できた**
- これでフェーズ3「文字を翻訳する」は完了。次はフェーズ4「文字を音声にする（音声合成）」に進む

### フェーズ4：文字を音声にする（音声合成）を開始

- システムライブラリ `espeak`、Pythonライブラリ `pyttsx3` をインストールした
  - `pyttsx3` はインターネット接続なしで動く音声合成ライブラリ
- `src/text_to_speech.py` を作成した
  - テキストを読み上げて音声ファイル（output.mp3）として保存する機能
- 開発サーバー上で実行を試したところ、Linux環境の音声エンジン（espeak）とpyttsx3の相性問題によりエラーが発生した
  - 【重要な制約】これは開発サーバー（Linux）特有の問題であり、Windowsでは`pyttsx3`がWindows標準の音声合成機能（SAPI5）を自動的に使うため、通常この問題は発生しないと考えられる
  - → 実機での動作確認はユーザー自身のパソコン（Windows）で行う必要がある

### ユーザーのWindows PCでの実機テスト（成功）

- `text_to_speech.py` をダウンロードし、`pip install pyttsx3` を実行してライブラリを導入した
  - Windowsでは `pywin32` など関連パッケージも自動的にインストールされた
- `python text_to_speech.py` を実行し、成功
  - 表示：「読み上げるテキスト: Hello, the weather is nice today.」「音声ファイルを保存しました: output.mp3」
  - スピーカーからのその場の読み上げ音声は聞こえなかったが、これはコードが `save_to_file`（ファイル保存）のみを行い `say`（その場で再生）を呼んでいなかったための想定通りの動作
  - 保存された `output.mp3` を再生したところ、正しく英語の音声（"Hello, the weather is nice today."）が再生されることを確認できた
- これで**音声合成（テキスト→音声ファイル）が実際に動くことを確認できた**
- これでフェーズ4「文字を音声にする（音声合成）」は完了。次はフェーズ5「リアルタイム化」に進む

### フェーズ5：リアルタイム化を開始

- `src/speech_to_translated_speech.py` を作成した
  - フェーズ2（音声認識）、フェーズ3（翻訳）、フェーズ4（音声合成）の3つの関数をひとつなぎにした
  - 「マイクで話す → テキスト化 → 翻訳 → その場で読み上げ」という一連の流れを実行する
  - 音声合成部分は、これまでの「ファイル保存」から「その場で読み上げる（`engine.say`）」に変更した
- 開発サーバーにはマイクが無く音声出力の相性問題もあるため、ここでは実行確認はせず、ユーザーのPCでの実機テストに進む

### ユーザーのWindows PCでの実機テスト（成功）

- `speech_to_translated_speech.py` を実行し、追加インストール無しで動作
- 一連の流れが成功
  - 話しかけた内容：「今日は暑いですね」
  - 認識結果：「今日は暑いですね」
  - 翻訳結果：「It's hot today, isn't it?」
  - スピーカーから英語の音声が読み上げられることを確認
- **「話す→認識→翻訳→読み上げ」の一連の流れが実際に動くことを確認できた**
- ユーザーからのフィードバック
  - 読み上げ音声が本人の声ではない → 想定通り。pyttsx3はWindows標準の機械的な合成音声を使っているのみで、本人の声の再現はフェーズ6で対応予定
  - 英語の発音が片言 → オフラインの基本的な音声合成エンジンのため。より自然な音声にするには、今後より高品質な音声合成エンジンへの変更を検討する
- これでフェーズ5「リアルタイム化」は完了。次はフェーズ6「本人の声を再現する」に進む

### フェーズ6：本人の声を再現する を開始

- 方針として、「Coqui XTTS」という声のクローン技術を採用することを決定
  - 声のクローンと英語発音の自然さの両方を、この1つの技術で同時に実現できるため
  - 事前学習は不要で、10秒程度の声のサンプル（wavファイル）を渡すだけで、その声質をまねて読み上げてくれる
- Pythonライブラリ `coqui-tts` をインストールした
- `src/clone_voice_speak.py` を作成した
  - 声のサンプルファイルと読み上げたいテキストを指定すると、その声質で読み上げた音声ファイルを生成する機能
- 【重要な制約】XTTSは初回実行時にAIモデル（1〜2GB）をインターネットからダウンロードする必要があるが、開発サーバーはこの種の外部ネットワーク接続が制限されているため、ここでは実行確認ができない
  - → 実機での動作確認は、ユーザー自身のパソコン（インターネット接続があり、自分の声を録音したファイルを用意できる環境）で行う必要がある

### 声のクローン（XTTS）の環境構築でのトラブルシューティング

ユーザーのWindows PCで `clone_voice_speak.py` の実機テストに挑戦。以下の順でエラーと対処を重ねた。

1. `ImportError: PyTorch and Torchaudio libraries ... not found` → `pip install torch torchaudio` で解決
2. `ImportError: cannot import name 'isin_mps_friendly' ...` → transformersのバージョン不整合。`pip install "transformers==4.49.0"` を試すも、逆に古すぎて別のエラーへ
3. `ImportError: cannot import name 'is_torchcodec_available' ...` → `pip install "transformers==4.57.0"` で解決
4. `ImportError: torchcodec library is required for audio IO` → `pip install coqui-tts[codec]` で解決
5. ライセンス確認プロンプト（CPML非商用ライセンスへの同意）→ `y` で同意し通過
6. `OSError: Could not load this library: libtorchcodec_core5.dll` / `FileNotFoundError: libtorchcodec_core4.dll` → torchcodecが必要とするFFmpegの共有ライブラリがWindows環境に無いことが原因と判明

【判断】ここまでで、XTTSによる声のクローンは依存関係の解決に時間がかかりすぎると判断し、方針を転換。
- 声のクローン（本人の声の再現）は**いったん保留**とし、今後の発展課題として`CURRENT_STATE.md`に記録する
- 先に「発音の自然さ」の改善を優先し、より軽量でインストールが簡単な `edge-tts`（Microsoftの高品質な音声合成サービス）に切り替えることを決定

### フェーズ6（改訂版）：edge-ttsによる自然な発音への切り替え

- Pythonライブラリ `edge-tts` をインストールした
- `src/natural_text_to_speech.py` を作成した
  - Microsoftの音声合成サービスを使い、pyttsx3よりも自然な発音で読み上げる機能
  - 声質は本人の声ではなく、Microsoftが提供する音声（例：`en-US-GuyNeural`）
- 開発サーバー上で実行を試したところ、外部サービス（speech.platform.bing.com）への接続が制限されているため失敗した
  - → こちらも実機での動作確認はユーザー自身のパソコンで行う必要がある

### ユーザーのWindows PCでの実機テスト（成功）

- `natural_text_to_speech.py` をダウンロードし、`pip install edge-tts` を実行してライブラリを導入（軽量なため問題なくスムーズに完了）
- `python natural_text_to_speech.py` を実行し、`output_natural.mp3` を生成
- ユーザーが音声を確認し、「発音もいい感じ」とのフィードバックを得た
- **edge-ttsによる自然な発音での読み上げが実際に動くことを確認できた**
- これでフェーズ6「発音の自然さ」の目標は達成。「本人の声の再現（声のクローン）」は引き続き発展課題として保留し、次はフェーズ7「仕上げ」に進む

### フェーズ7：仕上げ（統合版）を開始

- `src/voicebridge.py` を作成した
  - `speech_to_translated_speech.py` をベースに、音声合成部分をpyttsx3からedge-ttsに差し替えた完成版
  - 加えて、生成した音声ファイルをWindows標準の方法（`os.startfile`）で自動再生する機能を追加した
  - 「マイクで話す → 認識 → 翻訳 → 自然な発音で自動再生」まで一気通貫で行う
- 開発サーバー上で文法チェック（構文エラーの有無）のみ実施し、問題なし
- 追加のライブラリインストールは不要（これまでにインストール済みのもので動作する見込み）
- 実機での動作確認はこれから

### ユーザーのWindows PCでの実機テスト（成功）

- `voicebridge.py` を実行し、追加インストール無しで動作
- 1回目は翻訳サービス側の一時的な不調で失敗したが、音声認識自体は完璧に動作（「今日は雨が土砂降りでしたね 台風が来ているらしいです」を正しく認識）
- 再実行したところ成功
  - 音声ファイルが自動的に開き、翻訳された自然な発音の英語音声が再生されることを確認
- **「話す→認識→翻訳→自然な発音で自動再生」という一連の流れが完全に動作することを確認できた**
- これでフェーズ7「仕上げ」の中核部分（機能の統合）は完了。**VoiceBridgeの基本形が完成した**

### 発展課題1：声のクローン（XTTS）への再挑戦（成功）

- ユーザーの希望により、保留していた声のクローンに再挑戦
- 前回の `libtorchcodec_core*.dll` エラーの原因を切り分け
  1. まず `winget install ffmpeg` でFFmpegをインストールしたが、バージョンが新しすぎて（v9）torchcodecと非互換のままエラーが継続
  2. 発想を変えて、`torchcodec` を必要としない少し古いバージョンの `torch`/`torchaudio`（2.5.1）に固定し直したところ、問題が解消
     - `pip install "torch==2.5.1" "torchaudio==2.5.1"`
- `python clone_voice_speak.py` を実行し、成功（表示：「完成しました: cloned_output.wav」）
- ユーザーが生成された音声を確認し、「音質が悪く聞き取りづらいが、たしかに自分の声に似ている」とのフィードバック
- **声のクローン（本人の声を再現した音声合成）が実際に動くことを確認できた。ただし音質には改善の余地あり**
  - 改善案：声のサンプルの録り直し（ノイズが少なく、はっきり長めに話したものにする）、出力のサンプリングレート調整などが考えられる

### 発展課題1（続き）：声のサンプルの改善を試行

- より長く（約30秒）、静かな環境で声のサンプルを録り直した
- 今回はユーザーのPCにffmpegがインストール済みだったため、ユーザー自身のPC上で以下のコマンドを実行し、XTTSが得意とする形式（24000Hz、モノラル）に変換した
  - `ffmpeg -y -i "（録音ファイル）" -ar 24000 -ac 1 my_voice_sample.wav`
- 変換したサンプルで `clone_voice_speak.py` を再実行し、`cloned_output.wav` を再生
- ユーザーからのフィードバック：「前よりはよくなっているが、音声の鮮明さに欠ける。録音状況のせいだと思うので、先に進めて大丈夫」
- **音質改善は前進したが、まだ完璧ではない。ユーザーの判断で、これ以上は追求せず次の発展課題へ進むことにした**
- これで発展課題1「声のクローン」への取り組みはひとまず区切り。次は発展課題2「対応言語を増やす」に進む

### 発展課題2：対応言語を増やす を開始

- `src/voicebridge_multilang.py` を作成した
  - `voicebridge.py` をベースに、日本語固定・英語固定だった部分を選択式にした
  - 対応言語：日本語、英語、韓国語、中国語（簡体字）、フランス語、スペイン語（6言語）
  - 実行時に「話す言語」「翻訳したい言語」を番号で選んでから使う
- 開発サーバー上で文法チェックのみ実施し、問題なし。追加のライブラリインストールは不要
- 実機での動作確認はこれから

### 発展課題2（続き）：全言語対応への拡張

- ユーザーの希望により、対応言語を「できる限り全言語」に拡張することを決定
- `src/voicebridge_alllang.py` を作成した
  - deep-translatorが対応する133言語すべてと、edge-ttsが対応する音声の一覧を、実行時に自動的に突き合わせて対応言語表を作る仕組みにした
  - 言語数が多いため、番号選択ではなく「言語名を英語で入力する」方式に変更（部分一致による候補表示にも対応）
  - この環境で `deep_translator` の対応言語数を確認したところ、133言語であることを確認できた（この部分はネットワーク不要で確認可能だった）
  - `edge_tts.list_voices()`（声の一覧取得）はネットワーク接続が必要なため、開発サーバーでは動作確認できず
- 実機での動作確認はこれから

### ユーザーのWindows PCでの実機テスト（成功）

- `voicebridge_alllang.py` を実行し、追加インストール無しで動作
- 起動時に「対応言語数: 71言語」と表示され、対応言語表の自動生成が正しく機能していることを確認
- 「japanese」「english」のように英語で言語名を入力する方式も問題なく動作
- 1回目は翻訳サービス側の一時的な不調で失敗したが、再実行で成功
- **全言語対応版（71言語の組み合わせ）が実際に動くことを確認できた**
- これで発展課題2「対応言語を増やす」は完了。次は発展課題3「使いやすい画面（UI）を作る」に進む

### 発展課題3：使いやすい画面（UI）を作る を開始

- ユーザーの希望を確認：スマホアプリとWebアプリの両方を求めていたが、開発規模を考慮し、以下の2段階方針に合意
  1. まずレスポンシブなWebアプリを作る（パソコン・スマホのブラウザ両方で使える）
  2. 後日、CapacitorなどのツールでそのWebアプリを実機にインストールできる形（ネイティブアプリ）に変換する
- `webapp/` フォルダにFlask製のWebアプリを作成した
  - `app.py`：バックエンド。翻訳（deep-translator）・音声合成（edge-tts）のAPIを提供する。起動時に対応言語表を自動生成する
  - `templates/index.html`：画面のHTML。マイクボタン、言語選択（入れ替えボタン付き）、会話履歴（吹き出し形式）を配置
  - `static/style.css`：深い紺色ベース＋琥珀色アクセントの、VoiceBridgeらしい落ち着いたデザイン
  - `static/app.js`：ブラウザ自体の音声認識機能（Web Speech API）でマイク入力を受け取り、サーバーに送って翻訳・音声合成結果を受け取る
  - 音声認識をPythonの`speech_recognition`からブラウザ側のWeb Speech APIに変更した（スマホのブラウザから直接パソコンのマイクにはアクセスできないため）
- 開発サーバー上でPython/JavaScriptの文法チェック、およびFlaskの `GET /` ルートの動作テスト（仮データ使用）を実施し、いずれも正常
- 実機での動作確認（実際にブラウザで開いて操作する）はこれから

### ユーザーのWindows PCでの実機テスト（成功、ファイル名の問題を修正）

- `webapp` フォルダを作成し、4つのファイルを配置して `pip install flask` → `python app.py` で起動
- 最初は500エラーが発生。原因はブラウザの二重ダウンロードにより `templates/index.html` が `index (1).html` という名前になっていたこと
  - `ren` コマンドでファイル名を修正し解決
- ブラウザ（Google Chrome）で `http://127.0.0.1:5000` にアクセスし、成功
  - マイクボタンを押して発話→認識→翻訳→吹き出し表示→自動再生まで、すべて正しく動作
- ユーザーからのフィードバック
  - 音声が声のクローンでないことを確認 → 想定通り（Webアプリ版は速度重視でedge-ttsの自然な声を使用する設計だったため）
  - 翻訳結果が日本語のまま表示された → ブラウザの自動翻訳機能が原因の可能性を案内
- **Webアプリ版（自然な声バージョン）が実際に動くことを確認できた**

### 発展課題3（続き）：声のクローン対応 + パスワード保護

- ユーザーの希望により、Webアプリにも声のクローンを組み込み、かつパスワードで保護することを決定
- `app.py` を全面的に更新した
  - 起動時に、声のサンプル（`voice_sample/my_voice_sample.wav`）を使ってXTTSモデルを一度だけ読み込む
  - XTTSが対応する17言語（英・日・韓・中・仏・独・伊・西など）は声のクローンで読み上げ、対応外の言語は自動的にedge-ttsの自然な声にフォールバックする
  - Flaskの`session`とパスワード認証を追加。正しいパスワード（`APP_PASSWORD`、初期値`voicebridge2026`）を入力しないとアプリを使えないようにした
  - `/login` ログイン画面、`/logout` ログアウト機能を追加
- `templates/login.html` を新規作成（ログイン画面）
- `static/style.css`、`static/app.js`、`templates/index.html` を更新
  - どちらの声（クローン／標準）で読み上げられたかを、会話履歴に小さなタグで表示するようにした
  - ヘッダーに「声のクローンが有効か」を表示し、ログアウトリンクも追加
- 開発サーバー上で以下をテストし、いずれも成功
  - Python/JavaScriptの文法チェック
  - パスワード保護機能（未ログイン時のリダイレクト、誤パスワード、正しいパスワードでのログイン）の簡易テスト
- 声のクローン自体（XTTSでの音声生成）は、開発サーバーの制約上テストできず。実機での動作確認はこれから

### ユーザーのWindows PCでの実機テスト（成功、ただし速度に課題）

- ファイルの配置後、`app.py`が更新されずしばらく古いバージョンのまま動いてしまうトラブルが発生
  - `Select-String -Path app.py -Pattern "APP_PASSWORD"` で新しいファイルが正しく配置されているか確認する方法で診断
  - 実際には新しいファイルは配置されていたが、モデル読み込み中に`Ctrl+C`を押してしまい処理が中断されていたことが判明。中断せず待つことで解決
- ログイン画面が正しく表示され、パスワード（`voicebridge2026`）でログインできることを確認
- マイクで発話→自分の声のクローンで読み上げられることを確認（「🎙️ あなたの声のクローンが有効です」の表示、実際にクローン音声で再生）
- **Webアプリでの声のクローン機能が実際に動くことを確認できた**
- ユーザーからのフィードバック：「翻訳、音声生成に時間がかかりすぎている」
  - 原因：XTTSはAIモデルが重く、GPU（グラフィックボード）が無い一般的なパソコンではCPUのみでの計算になり、生成に数秒〜数十秒かかることがある
  - 対策を検討中

### 発展課題3（続き）：Webアプリの音声を自然な声に戻す（速度優先）

- ユーザーの判断により、Webアプリでのリアルタイム会話には常に自然な声（edge-tts）を使い、
  声のクローンはコマンドライン版（`clone_voice_speak.py`）専用とすることを決定
- `app.py` を修正し、起動時の `load_voice_clone_model()` の呼び出しをコメントアウト
  - `XTTS_MODEL` が常に `None` のままになるため、`process()` 内の判定により自動的に常にedge-ttsが使われるようになる
  - パスワード保護の仕組み自体は、有用な機能としてそのまま維持
- 開発サーバー上で文法チェックを実施し、問題なし
- 実機での動作確認（速度が改善したか）はこれから

### 発展課題3（続き）：パスワード削除 + アプリ内での声の録音機能を追加

- ユーザーの希望により、パスワード保護機能を削除し、代わりにアプリ画面上で声を録音してクローン用のサンプルとして保存できる機能を追加することを決定
- `app.py` を全面的に書き換えた
  - パスワード保護（`/login`, `/logout`, `session`関連）をすべて削除
  - `/api/save-voice-sample` エンドポイントを新設：ブラウザから送られた録音データ（webm形式）を受け取り、`ffmpeg`でXTTS向けのWAV形式（24000Hz・モノラル）に変換して `voice_sample/my_voice_sample.wav` に保存する
  - 声のクローンAIモデル（XTTS）は起動時ではなく、「自分の声で読み上げる」が実際に使われた最初のタイミングで読み込む方式（遅延読み込み）に変更。これにより通常の起動は速いまま
  - `/api/process` に `use_clone` パラメータを追加。チェックが入っている時だけ声のクローンを使う（それ以外は高速なedge-tts）
- `templates/login.html` を削除
- `templates/index.html` を更新：ログイン関連の表示を削除し、「声を録音する」ボタン・声のサンプル登録状況の表示・「自分の声で読み上げる」チェックボックスを追加
- `static/style.css` を更新：録音ボタン、チェックボックスなどのスタイルを追加
- `static/app.js` を更新
  - ブラウザの `MediaRecorder` を使い、15秒間の録音→サーバーへのアップロードを行う機能を追加
  - 音声生成リクエストに `use_clone`（チェックボックスの状態）を含めるように変更
- 開発サーバー上でPython/JavaScriptの文法チェック、Flaskルートの簡易テスト（録音ボタンの存在、パスワード関連の記述が残っていないことなど）を実施し、いずれも成功
- 実機での動作確認（実際に録音してクローン音声を生成できるか）はこれから

### ユーザーのWindows PCでの実機テスト（成功、UI改善2件を依頼）

- 声の録音、クローン音声での読み上げが実際に成功
- 「やはりクローン音声による翻訳はかなり時間がかかる」との感想（想定通り、CPUのみでのXTTS処理のため）
- ユーザーから改善要望2件
  1. 話しかけている最中に間が空くと自動的に認識が区切られ、話が途中で切れてしまう。今の自動終了機能は残しつつ、自分でオン/オフを操作できる機能も追加してほしい
  2. 「自分の声で読み上げる」という文言が紛らわしいので「自分のクローン音声で読み上げる」に変更してほしい

### 発展課題3（続き）：手動録音モードの追加 + 文言修正

- `templates/index.html`：チェックボックスの文言を「自分のクローン音声で読み上げる」に変更。新たに「自分で録音の開始・終了を操作する」チェックボックスを追加
- `static/app.js` を更新
  - ブラウザの音声認識（Web Speech API）を、手動モードでは `continuous: true` にして無音でも自動終了しないようにし、通常モードでは今まで通り自動終了する仕様を維持
  - どちらのモードでも、マイクボタンをもう一度押すと認識を終了できるようにした（手動モードではこれが唯一の終了方法）
  - 認識結果をリアルタイムに蓄積・表示する仕組みに変更し、終了時にまとめてサーバーへ送信するようにした
- 開発サーバー上でPython/JavaScriptの文法チェック、および文言変更の反映確認を実施し、成功
- 実機での動作確認（手動モードで話の途中で切れずに録音できるか）はこれから

### ユーザーのWindows PCでの実機テスト（手動モード成功、新たな改善依頼）

- 手動モードは正常に動作し、「いい感じ」との評価
- 新たな改善要望：翻訳後の読み上げ音声自体に不自然な「間」がある
  - 原因を確認したところ、声のクローンが「録音時の話し方のクセ（間）」を真似してしまっていることが判明

### 発展課題3（続き）：声のサンプルから自動的に無音区間を除去

- `app.py` の `/api/save-voice-sample` における ffmpeg 変換コマンドに、`silenceremove` フィルタを追加
  - 声のサンプル録音時に含まれる無音区間（間）を自動的に取り除いてから保存するようにした
  - これにより、声のクローンが「間」を声の特徴と一緒に学習してしまうのを防ぎ、より流ちょうな読み上げになることを狙った
- 開発サーバー上で、無音を含むテスト音声（3秒）を作成し、フィルタ適用後に無音区間が正しく除去される（2.2秒に短縮）ことを確認
- 実機での動作確認（実際に声を録音し直し、読み上げが流ちょうになるか）はこれから

### ユーザーからの要望：品質改善のまとめ対応

- ユーザーから「鼻息もノイズとして認識してしまう」との指摘、および「細かく都度確認せず、翻訳アプリに必要な要素をまとめて構築してほしい」との依頼
- 以下をまとめて対応した
  1. **ノイズ除去**：`app.py`の声のサンプル変換フィルタに `highpass=f=120`（低音域のノイズ・鼻息をカット）、`afftdn=nf=-25`（全体的な背景ノイズ軽減）を追加。無音除去フィルタと合わせて使用
  2. **二重送信防止**：`app.js`で、翻訳・音声生成中や録音中はボタンを一時的に無効化するようにした
  3. **エラー処理の強化**
     - バックエンド（`app.py`）：不正なリクエスト形式、翻訳結果が空の場合などを個別にチェックし、分かりやすいエラーメッセージを返すようにした。声のクローンでの音声合成が失敗した場合は、自動的に自然な声（edge-tts）にフォールバックして再試行するようにした。予期しないサーバーエラー全般に対応する共通エラーハンドラーを追加
     - フロントエンド（`app.js`）：サーバーからの応答が不正な場合、音声の再生に失敗した場合など、細かいエラーケースにも分かりやすいメッセージを表示するようにした
- 開発サーバー上で以下をテストし、いずれも成功
  - ノイズ除去＋無音除去フィルタの組み合わせが正しく機能すること
  - Python/JavaScriptの文法チェック
  - 各種エラーケース（空のテキスト、未対応言語、不正なリクエスト形式）で適切なエラーメッセージが返ること
- 実機での動作確認（ノイズが減ったか、実際の使用感）はこれから

### ユーザーのWindows PCでの実機テスト（翻訳エラーページの誤表示を発見）

- 実際に会話を試したところ、翻訳結果として「Error 500 (Server Error)!!1500.That's an error...」というGoogleのエラーページの文言がそのまま表示される不具合が発生
- 原因：Google翻訳側が一時的にエラーページを返した際、`deep-translator`がそれを正常な翻訳結果として扱ってしまい、既存のチェック（結果が空でないか）をすり抜けていたため

### 発展課題3（続き）：翻訳エラーページの自動検知＋自動リトライ

- `app.py` に `looks_like_translation_error()` 関数を追加
  - 翻訳結果に「Error 500」「That's an error」「<html」などのエラーページ特有の文言が含まれていないかチェックする
- `translate_with_retry()` 関数を追加
  - 翻訳結果がエラーページっぽい場合や、翻訳自体が例外で失敗した場合、1秒待ってから最大3回まで自動的に再試行する
  - `process()` 内の翻訳呼び出しをこの関数に置き換えた
- 開発サーバー上で以下をテストし、いずれも成功
  - エラーページのテキストが正しく検知されること、正常な翻訳結果は誤検知されないこと
  - 1回目にエラーページが返っても、自動的に再試行して2回目で正常な結果を取得できること（モックを使ったテスト）
- 実機での動作確認はこれから

### 発展課題3（続き）：アプリ名変更 + 原文・翻訳文それぞれの再生ボタン追加

- ユーザーの希望により、アプリの表示名を「ほんやくコンニャク」に変更（内部のプロジェクト名 VoiceBridge やファイル名は変更していない）
  - `templates/index.html` の `<title>` と見出しを変更
- `app.py` の `/api/process` を更新し、翻訳後の文章だけでなく、**原文の音声も生成する**ようにした
  - 原文の音声は速度を優先し、常に自然な声（edge-tts、話す言語に対応した声）で生成する
  - レスポンスを `audio_url` から `original_audio_url` と `translated_audio_url` の2つに分割した
- `static/app.js` を更新
  - リクエストに `source_language` を含めるようにした（原文の音声にどの言語の声を使うか判定するため）
  - 会話履歴の吹き出し（原文・翻訳文）それぞれに、個別の再生ボタン（▶アイコン）を追加した
  - 従来通り、翻訳後の音声は自動再生されるが、原文・翻訳文どちらも後から何度でも再生ボタンで聞き直せるようにした
- `static/style.css` に再生ボタンのスタイルを追加
- 開発サーバー上でPython/JavaScriptの文法チェック、および `/api/process` が新しいレスポンス形式（`original_audio_url`, `translated_audio_url`）を正しく返すことをモックテストで確認し、成功
- 実機での動作確認はこれから

### ユーザーからの指摘：原文の音声がクローンになっていない

- ユーザーから「翻訳前の文章が片言すぎるし、クローン音声になっていない」との指摘
- 原因：`app.py`で、原文の音声は常に自然な声（edge-tts）を使う設計にしていたため（意図的な設計だったが、ユーザーの希望に合っていなかった）

### 発展課題3（続き）：原文の音声にもクローンを適用

- `app.py` を修正し、「自分のクローン音声で読み上げる」のチェックが入っている場合は、**原文・翻訳文の両方**にクローン音声を使うようにした
  - 翻訳先の言語がXTTS非対応でも、話す言語（原文側）がXTTS対応なら、原文だけクローン音声にできるよう、モデルの読み込み判定を修正
  - クローンで失敗した場合は、原文・翻訳文それぞれ個別に自然な声へフォールバックする
- 開発サーバー上でモックテストを実施し、原文・翻訳文の両方が正しい言語コードでXTTSに渡されることを確認
- 実機での動作確認はこれから

### 追加機能の優先順位を決定

- ユーザーから「世に流通している翻訳アプリの機能一覧」の依頼を受け、Google翻訳・DeepL・Papagoなどを調査し、カテゴリ別に整理して提示した
  （テキスト翻訳、会話モード、カメラ翻訳、Webページ翻訳、オフライン対応、履歴保存など）
- 利便性・使用頻度をもとに、以下の優先順位で実装することに決定
  1. テキスト入力での翻訳（音声が使えない時の代替）
  2. 翻訳履歴の保存
  3. よく使うフレーズの保存（お気に入り）
  4. カメラでの文字認識翻訳（OCR）
  5. オフライン対応（優先度低）

### 発展課題3（続き）：優先度1「テキスト入力での翻訳」を実装

- `templates/index.html` に、マイクボタンの下にテキスト入力欄と送信ボタンを追加
- `static/app.js` にフォーム送信のイベントリスナーを追加し、入力したテキストを既存の `sendToServer()` にそのまま渡す仕組みにした（音声認識を経由しないだけで、翻訳・音声合成の処理は共通）
- `static/style.css` にテキスト入力欄のスタイルを追加
- 開発サーバー上でPython/JavaScriptの文法チェック、テキスト入力欄がHTMLに含まれることを確認
- 実機での動作確認はこれから

### ユーザーからの再指摘：原文の音声がクローンになっていない（修正後も再現）

- 「原文にもクローンを適用」の修正を反映した`app.py`であることを`Select-String`で確認済み（正しく配置されている）
- サーバーログを確認したところ、翻訳文の音声は`.wav`（クローン、期待通り）で生成されているが、原文の音声は`.mp3`（自然な声、期待と異なる）になっていることが判明
- 静かに自然な声へフォールバックしていた（`except`ブロックで例外の内容を握りつぶしていたため、原因が画面に出ていなかった）

### 発展課題3（続き）：原因特定のための診断ログを追加

- `app.py`に診断用の`print`文を追加した
  - 原文クローンの判定条件（`want_clone_voice`, `model_loaded`, `source_xtts_code`, `use_clone_for_original`）を表示
  - 原文のクローン生成で例外が発生した場合、その内容（種類とメッセージ）を表示
- 次にユーザーのPCで再度試してもらい、ターミナルに表示される診断ログの内容から原因を特定する予定

### 原因判明：日本語専用ライブラリ（cutlet）の不足

- ユーザーのPCで診断ログを確認したところ、`ModuleNotFoundError: No module named 'cutlet'` が原因と判明
  - XTTSが日本語のテキストを処理する際に必要な補助ライブラリが不足していた（英語などでは使われないため、今まで気づかれなかった）
- `pip install cutlet fugashi unidic-lite` をユーザーのPCで実行し、インストール成功
- 再テストの結果、原文・翻訳文の両方でクローン音声が使われることを確認できた

### ユーザーからの追加フィードバック：原文（日本語）の発音がまだ片言

- クローン自体は機能しているが、日本語の発音の自然さについてはまだ改善の余地があるとの指摘
- 原因は、XTTSというAIモデル自体が英語・ヨーロッパ言語を中心に開発されており、日本語の合成品質は他の言語と比べてやや劣る傾向があるという、モデル自体の既知の制約である可能性が高いことをユーザーに説明
- 改善を試みるため、`app.py`の`tts_to_file()`呼び出しに以下のパラメータを追加した
  - `split_sentences=True`：長い文章を文単位に区切って生成し、自然さを保ちやすくする
  - `speed=0.95`：わずかに読み上げ速度を落とし、発音の崩れを抑える
- 併せて、原因調査のために追加していた診断用の`print`文は、原因特定後に不要になったため削除した
- 開発サーバー上で文法チェックを実施し、問題なし。実機での効果確認はこれから

### ユーザーからの追加要望：言語ごとに最適な声のクローン技術を使いたい

- ユーザーから「各言語に適したクローン技術を使いたい」との要望
- 技術的なリスク（新たなAIモデル一式の追加インストールによる、これまでと同様の依存関係の衝突リスク、GPUが無い環境での速度低下）を説明した上で、ユーザーの判断を確認
- ユーザーの判断により、**今回は見送り**、現状のXTTSを土台として進めることに決定。他の機能追加を優先する

### 発展課題3（続き）：優先度2「翻訳履歴の保存」を実装

- `app.py` に会話履歴の保存・取得・削除機能を追加
  - `history.json` というファイルに、会話のやり取り（原文・翻訳文・使用した言語・音声ファイルの場所・クローン使用有無・日時）を自動的に記録する
  - `/api/history`（GET）：保存されている履歴を返す
  - `/api/history`（DELETE）：履歴をすべて削除する
  - `/api/process` の処理の最後に、履歴への自動保存を追加した
  - 保存件数が増えすぎないよう、最大300件までの上限を設定
- `templates/index.html` に「履歴を削除」ボタンを追加
- `static/app.js` を更新
  - ページ読み込み時に `/api/history` から過去の履歴を取得し、会話履歴として表示するようにした（ページを再読み込みしても、過去の会話が消えなくなった）
  - 「履歴を削除」ボタンで、確認ダイアログを出したうえで履歴を全削除できるようにした
- `static/style.css` にスタイルを追加
- 開発サーバー上で、履歴の保存・取得・削除機能が正しく動作することをモックテストで確認し、成功
- 実機での動作確認はこれから

### ユーザーからの追加要望：単文削除機能 + 明るいUI

- ユーザーから「一括削除だけでなく、履歴を1件ずつ削除したい」「サイトが暗くて見づらいので、明るい色調にしてほしい」との要望

### 発展課題3（続き）：履歴の単文削除機能を追加

- `app.py` を更新
  - 各履歴に一意のID（`uuid`）を持たせるようにした
  - `/api/history/<entry_id>`（DELETE）エンドポイントを新設し、指定した1件だけを履歴から削除できるようにした
  - `/api/process` のレスポンスにも `id` を含めるようにした（会話直後にも、その場で削除ボタンを使えるように）
- `static/app.js` を更新
  - 各会話の吹き出しペアの下に「この履歴を削除」ボタンを追加。確認ダイアログの後、該当のIDだけをサーバーに削除依頼し、成功したら画面からもその部分だけを取り除くようにした
- 開発サーバー上でモックテストを実施し、指定したIDの履歴だけが正しく削除され、他は残ることを確認

### 発展課題3（続き）：UIを明るい配色に一新

- `static/style.css` を全面的に書き換え、暗い紺色ベースの配色から、明るい寒色系（淡い青灰色の背景、白いパネル、深みのある琥珀色のアクセント、深緑がかったティール）の配色に変更した
  - よくある「生成AIっぽい配色」（暖色系のベージュ+テラコッタなど）を避け、爽やかで読みやすい配色を意識した
  - ボタン・入力欄・吹き出し・カードなど、すべての要素の色を新しい配色に合わせて調整
- 開発サーバー上で、新しい配色（背景色`#F3F6F8`）がCSSに反映されていることを確認
- 実機での見た目の確認はこれから

### ユーザーからの追加要望：タイトルのデザイン変更 + スマホからの外部アクセス

- ユーザーから「ほんやくコンニャク」のフォントをドラえもんの秘密道具っぽくしてほしい、スマホでもアクセスできるようにしてほしい（開発段階なのでリンクを知っている人だけで良い）との要望

### 発展課題3（続き）：タイトルを「秘密道具ラベル」風デザインに変更

- Googleフォント「M PLUS Rounded 1c」（丸ゴシック体）を読み込むように変更
- `templates/index.html` のタイトル部分を、丸いバッジ（ラベルプレート）で囲むデザインに変更
- `static/style.css` に `.gadget-badge` のスタイルを追加（緑系のグラデーション、立体的な影、小さな「つまみ」の装飾）
- 開発サーバー上でCSSの構文チェックを実施し、問題なし

### 発展課題3（続き）：スマホからの外部アクセス方法を案内

- これまでの「同じWi-Fi内でのアクセス」に加え、外出先など別のネットワークからもアクセスできるようにする方法として、`ngrok`（無料のトンネルサービス）の利用手順を案内した
  - ngrokに登録し、認証トークンを設定した上で `ngrok http 5000` を実行すると、外部からアクセスできるランダムなURL（例: `https://xxxx.ngrok-free.app`）が発行される
  - このURLを知っている人だけがアクセスできる（開発段階の共有としては十分だが、本格的なセキュリティではないことを説明）
  - URLは再起動のたびに変わること、Webアプリとngrokの両方を起動したままにする必要があることを注意点として伝えた
- コード変更は無し（環境構築・外部ツールの利用方法の案内のみ）

### ユーザーのWindows PCでのngrokセットアップ（成功）

- ngrokへの登録、Windows用インストーラー（Microsoft Store経由の`winget install ngrok -s msstore`）のインストール、認証トークンの登録までを順番にサポートしながら実施
- `python app.py` でWebアプリを起動したまま、別ウィンドウで `ngrok http 5000` を実行し、外部アクセス用URL（`https://snowshoe-fencing-skydiver.ngrok-free.dev`）を取得
- スマホのブラウザでこのURLを開き、**「ほんやくコンニャク」に外部からアクセスできることを確認**
- **これで、開発中のアプリをスマホなど別のネットワークから使えるようになった**（ngrokを再起動するたびにURLが変わる点、パソコン側でアプリとngrokの両方を起動したままにする必要がある点は継続的な注意点）

### ユーザーからの追加要望：パソコンの電源なしで常時アクセス（有料クラウドデプロイ）

- ユーザーから「パソコンの電源が切れていても常にアクセスできるようにしたい」との要望
- Render.comの料金プランを調査し、費用感を提示
  - 無料〜$7プラン：メモリ512MBのため、声のクローン（XTTS）は動作しない
  - Standardプラン（$25/月、2GBメモリ）：声のクローンが動く可能性はあるが、メモリ不足のリスクあり
  - Proプラン（$80/月、4GBメモリ）：余裕を持って動作する見込み
- 「なぜパソコンなら無料でクラウドなら有料なのか」という質問に対し、計算資源を借り続けることに費用がかかる仕組みであることを説明。無料の代替案（パソコン+ngrok）も改めて提示したが、ユーザーは「パソコンの電源が切れていても使いたい」ため有料デプロイを希望
- 予算は月額$25（Standardプラン）で試すことに決定

### 発展課題3（続き）：Renderへのデプロイ準備

- `app.py` を更新
  - 環境変数 `APP_PASSWORD` が設定されている場合のみパスワード保護を有効にする仕組みに変更（ローカルでは保護なし、クラウドでは保護ありに切り替えられる）
  - 環境変数 `DATA_DIR` で、声のサンプル・履歴の保存先を指定できるようにした（Renderの永続ディスクに対応するため）
  - 環境変数 `PORT` に対応（Renderが動的に割り当てるポート番号を使えるようにするため）
  - `gunicorn`（本番用サーバー）からモジュールとして読み込まれた場合にも、対応言語の準備が実行されるよう修正
  - 起動時のネットワークエラーでアプリ全体がクラッシュしないよう、例外処理を追加
- `templates/login.html` を復活させた（クラウド版でのパスワード画面用）
- `requirements.txt` を新規作成（必要なPythonライブラリの一覧）
- `Dockerfile` を新規作成
  - Python 3.12 + ffmpegの環境を構築し、`gunicorn`でアプリを起動する内容
  - 声のクローンAIのライセンス確認を自動化する設定（`COQUI_TOS_AGREED=1`）を含む
- `.gitignore` を新規作成（生成音声・履歴・声のサンプルなど、個人情報を含むファイルをGitHubにアップロードしないようにするため）
- 開発サーバー上で、パスワード保護機能（未設定時は保護なし、設定時はログイン画面が出る）と、ネットワークエラー時にアプリがクラッシュしないことをテストし、成功
- Dockerfile自体のビルドテストは、開発サーバーにDockerが無いため実施できず。実際のデプロイ作業はこれから

### ユーザーの目標転換：本格的な公開サービス化を検討

- ユーザーから「アプリを完成させ、正式にデプロイしてサービスとして公開したい」との要望
- 重要な問題を指摘：現在使用しているXTTS（声のクローン）は**非商用限定ライセンス（CPML）**であり、不特定多数が使う公開サービスにはそのままでは使えないことを説明
- 併せて、翻訳機能が非公式なGoogle翻訳の仕組みに依存している点、ユーザー管理が無い点、プライバシーポリシー・利用規約が無い点、同時アクセスに耐える設計になっていない点など、公開サービス化に必要な要素を整理して提示
- 5段階のロードマップ（①法的基盤 → ②複数ユーザー対応 → ③プライバシー・規約整備 → ④同時アクセス対応 → ⑤ビジネスモデル検討）を提示し、フェーズ1・3は技術だけでは解決できず、ユーザー自身の意思決定・専門家相談が必要であることを伝えた
- ユーザーの判断で、まず「フェーズ2：複数ユーザー対応の技術基盤」から着手することに決定

### 発展課題（新）：複数ユーザー対応の技術基盤を構築

- `db.py` を新規作成し、SQLite（軽量なファイル完結型データベース）を使った複数ユーザー対応の基盤を実装
  - `users`テーブル：ユーザー名、パスワードのハッシュ値（`werkzeug.security`で安全に暗号化）、登録日時
  - `history`テーブル：会話履歴。`user_id`で紐づけることで、利用者ごとにデータを分離
  - ユーザー登録（`create_user`、ユーザー名の重複防止）、ログイン認証（`verify_user`）、履歴の追加・取得・削除（利用者ごとにスコープされる）、声のサンプルファイルのパスを利用者ごとに分離する仕組み（`voice_sample_path_for_user`）を実装
- `app.py` を大幅に更新
  - 従来の「共有パスワード1つ」の認証方式から、`/register`（新規登録）・`/login`（ログイン）・`/logout`（ログアウト）による、利用者ごとのアカウント認証方式に全面的に切り替えた
  - 声のサンプル、会話履歴のすべての処理を、ログイン中の利用者（`session["user_id"]`）に紐づけるように変更
  - 以前のJSON履歴保存関連の関数は削除し、`db.py`の関数に置き換えた
- `templates/register.html` を新規作成（新規登録画面）
- `templates/login.html` を更新（ユーザー名入力欄を追加、新規登録画面へのリンクを追加）
- `templates/index.html` を更新（ログイン中のユーザー名表示、ログアウトリンクを追加）
- `static/style.css` にログイン・登録画面のリンクのスタイルを追加
- `.gitignore` にデータベースファイル（`voicebridge.db`）を追加
- 開発サーバー上で、以下を包括的にテストし、すべて成功
  - データベース単体のテスト（ユーザー登録、重複防止、パスワード認証の正誤判定、履歴の分離）
  - Flask経由での新規登録〜ログイン〜ログアウトの一連の流れ
  - モックを使った、2人の異なるユーザーが会話した際、それぞれの履歴が正しく分離されて保存・取得されること（実際に「taro」「hanako」2アカウントで検証し、互いの履歴が混ざらないことを確認）
- 実機での動作確認はこれから

### GitHub連携によるファイル同期の自動化

- ユーザーから「毎回ファイルを手動でダウンロード・上書きするのが面倒」との相談
- 選択肢として「ZIPでまとめて渡す」「GitHub経由で自動同期する」を提示し、後者（どうせ後でRenderデプロイに必要になるため）を選択
- ユーザーのGitHubアカウント作成、非公開（Private）リポジトリの作成、Fine-grained Personal Access Token（Contents: Read and write権限）の発行までをサポート
- 開発サーバー上でトークンを使い、`git clone`でリポジトリへの接続を確認 → `webapp`フォルダの中身（`.gitignore`により声のサンプル・履歴・生成音声は除外）をコミットし、`git push`でアップロードすることに成功
- ユーザーのパソコン側でも、GitとリポジトリのcloneでVoiceBridgeのコードを取得し、以降は `git pull` だけで最新化できる仕組みを案内した
- **今後、Claudeがコードを変更した際は、都度GitHubへpushする運用に切り替える**（ファイルを個別にダウンロードしてもらう手間を削減するため）
- セキュリティ上の注意：会話内で共有されたアクセストークンは、作業完了後に失効させることをユーザーに推奨した

### ユーザーのWindows PCでの実機テスト（複数ユーザー対応、成功）

- Gitはすでにインストール済みだったため、そのまま`git clone`でリポジトリを取得
- 新しい`webapp`フォルダで起動し、以下が正常に動作することを確認
  - アカウント登録・ログイン（「ともき さんとしてログイン中」の表示）
  - 声のサンプルの登録（利用者専用のファイル名に変更されたため、録り直しが必要だった。想定通り）
  - 「自分のクローン音声で読み上げる」「自分で録音の開始・終了を操作する」の両方にチェックを入れての会話成功
  - 会話履歴の表示、原文・翻訳文それぞれの再生ボタン
- **複数ユーザー対応の技術基盤が、実機で正しく動作することを確認できた**
- ユーザーからの新たな指摘
  1. 原文（日本語）の発音が片言 → 既知のXTTSの日本語対応の限界（以前から説明済み）
  2. 翻訳文を再生すると、最後に間や雑音が入る → 新しい問題

### 発展課題（続き）：クローン音声の末尾トリミング処理を追加

- `app.py` に `trim_trailing_noise()` 関数を追加
  - ffmpegの`silenceremove`（末尾の無音除去）と`afade`（ごく短いフェードアウト）を組み合わせ、
    XTTSが生成した音声の末尾に残りがちな不要な「間」やノイズ、プツッという切れ目の音を取り除く
  - 翻訳文・原文、両方のクローン音声生成後に適用するようにした
- 開発サーバー上で、末尾に無音を含むテスト音声（2.5秒）を使い、トリミング後に約1.27秒に短縮されることを確認
- GitHubリポジトリへの反映が完了（今回から、Claudeが変更するたびに都度pushする運用を開始）
- 実機での効果確認はこれから（ユーザーは`git pull`で最新化できる）

### 重大な不具合の発見と修正：末尾トリミング処理が音声を壊してしまう

- ユーザーから「改善どころか、クリック音のような一瞬の音しか再生されなくなった」との報告
- 開発サーバー上で、文中に短い間があるパターンの音声を使って再現テストを実施し、バグを特定
  - `stop_periods=1` の設定では、「末尾」ではなく「文章中の最初の間（区切りの間）」で音声がバッサリ切られてしまうことが判明
- 正しい方法（音声を逆再生 → 先頭（＝元の音声では末尾）の無音だけを削除 → もう一度逆再生して元に戻す、というffmpegの定番テクニック）に修正
- 開発サーバー上で、文中の間（0.3秒）は保持したまま、末尾の無音（1秒）だけが正しくトリミングされることを確認（3.3秒 → 2.3秒）
- 修正版をGitHubにpush（今回は正常な通常pushで完了、force pushの問題は再発せず）
- 実機での効果確認はこれから

### 末尾トリミング処理の緊急無効化 + 新たな不具合（余計な言葉の生成）

- ユーザーから「文章の再生すらまともにできていない」との報告を受け、`trim_trailing_noise()`の呼び出しをコメントアウトして緊急無効化。GitHubにpushし、元の動作する状態に復元した
- ユーザーが復元後の動作を確認したところ、音声再生自体は正常に戻ったが、「翻訳文の再生で、文章に無い言葉が追加で再生される」という別の問題を発見
  - これはXTTSというAIモデル自体が時々起こす、既知の「ハルシネーション（幻聴のような余計な音声生成）」現象である可能性が高いことを説明
- 対処として、`split_sentences=True`（文単位に区切って生成する設定）を`False`に変更し、区切り目での余計な音声混入が減るか試すことにした
- GitHubにpush済み。実機での効果確認はこれから

### 翻訳エンジンをDeepL公式APIに切り替え

- 「本格的な事業化を見据えて、まずは現実的な範囲で品質・信頼性を上げたい」というユーザーの方針を受け、
  非公式なGoogle翻訳（deep-translator経由）から、DeepL公式APIへの切り替えを決定
- DeepL APIの料金体系を調査：無料枠は月50万文字（新規登録で利用できるかは要確認）、
  有料でも100万文字あたり$5.49と、VoiceBridgeの想定利用量なら十分安価であることを確認
  - トレードオフ：対応言語がこれまでの71言語から、DeepL対応の31言語に減る
- ユーザーにDeepL APIアカウントの作成・APIキー取得を依頼（コードには直接書き込まず、環境変数`DEEPL_API_KEY`で管理する方針）
- `app.py` を更新
  - `deep_translator.GoogleTranslator` を `deepl.Translator` に置き換え
  - `translate_with_retry()`：DeepL APIを呼び出す形に変更。正式なAPIのため、以前の「エラーページを翻訳結果と誤認する」問題への対策（`looks_like_translation_error`）は不要になり削除した
  - `build_language_table()`：DeepLの対応言語一覧（`get_target_languages()`）とedge-ttsの声を突き合わせる形に変更
- `requirements.txt` に `deepl` ライブラリを追加、`deep-translator` を削除
- 開発サーバー上で、モックを使って翻訳呼び出し・言語一覧作成の両方をテストし、いずれも正しく動作することを確認
- GitHubにpush済み。実際のAPIキーでの動作確認、対応言語数の変化の確認はこれから

### 声のクローンをXTTSからElevenLabs公式APIに切り替え

- 「原文・翻訳文ともにクローン音声の発音がいまいち」というユーザーの指摘を受け、
  無料・非商用限定のXTTSを、商用の高品質サービス「ElevenLabs」に切り替えることを提案し、了承を得た
- ElevenLabsの料金を調査：Starterプラン月$5から商用ライセンス付きの声のクローンが利用可能。29言語以上に対応
- 実装前に、ElevenLabs公式Pythonライブラリの正確なAPI構造（`voices.ivc.create`、`text_to_speech.convert`、`voices.delete`など）を
  `inspect`モジュールで直接確認してから実装に着手（過去の勘違いによる不具合の反省を踏まえた慎重な進め方）
- `db.py` を更新：`users`テーブルに `elevenlabs_voice_id` 列を追加し、利用者ごとにElevenLabs上の声のID（voice_id）を保存・取得できるようにした
- `app.py` を全面的に更新
  - XTTS関連のコード（`ensure_xtts_loaded`、`XTTS_SUPPORTED_LANGS`、`trim_trailing_noise`など）をすべて削除
  - `clone_voice_from_sample()`：声のサンプルをElevenLabsにアップロードし、声のクローンを作成（登録済みの場合は古い声を削除してから作り直す）
  - `speak_with_cloned_voice()`：登録した声のIDを使って、ElevenLabsの多言語モデル（`eleven_multilingual_v2`）でテキストを読み上げる
  - `/api/save-voice-sample`：録音後、ffmpeg変換に続けてElevenLabsへの声のクローン登録を行うように変更
  - `/api/process`：声のクローン判定を、XTTSのモデル読み込みではなく、ElevenLabsに登録済みの声のIDの有無で行うように変更
- `requirements.txt` を更新：`coqui-tts`, `torch`, `torchaudio`, `cutlet`, `fugashi`, `unidic-lite`（XTTS関連の重量級ライブラリ）を削除し、`elevenlabs`を追加
  - これにより、依存関係が大幅に軽量化された（これまで何度も苦しめられたPyTorch関連のインストール問題が無くなる）
- `Dockerfile` から、XTTSライセンス自動同意の設定（`COQUI_TOS_AGREED`）を削除し、gunicornのタイムアウトも短縮（クローン生成が速くなるため）
- 開発サーバー上で、ElevenLabs公式ライブラリの構造をinspectで確認したうえで、以下をモックテストし、すべて成功
  - 声のクローン作成・データベースへの保存
  - 作成した声のIDを使った読み上げ
  - `/api/save-voice-sample` から `/api/process` までの一連の流れ（`voice_cloned: True` が正しく返ることを確認）
- GitHubにpush済み。ユーザーにElevenLabs APIキーの取得を依頼中。実際のAPIキーでの動作確認はこれから

### ElevenLabs無料プランでの制約発覚 + Dubbing機能の調査

- ユーザーがElevenLabsに登録（無料プランのまま）し、声のサンプル登録を試したところ、
  「Your subscription does not include instant voice cloning. Please upgrade your plan.」というエラーが発生
  - 無料プランには声のクローン機能が含まれておらず、Starterプラン（月$5）以上が必要であることを実機で確認
- ユーザーから「翻訳と声のクローンを両方行えるサービスはないか」との質問を受け、ElevenLabsの「Dubbing（吹き替え）」機能を調査
  - 90以上の言語に対応し、話者の声を保ったまま翻訳・吹き替えができる機能だが、動画・長尺音声向けに設計されており、
    VoiceBridgeのような短い会話のやり取りには不向きと判断。今回は見送り、DeepL+ElevenLabsテキスト読み上げの组み合わせを継続することにした

### プロバイダー差し替え可能な設計へのリファクタリング

- ユーザーから「ランニングコストがかかるのは避けたいので、途中で別の手法に切り替えられるように設計してほしい」との要望
- `providers/` という新しいフォルダを作成し、翻訳・声のクローンの処理を「プロバイダー」として切り出した
  - `providers/translation.py`：`DeepLTranslationProvider` クラスと、環境変数 `TRANSLATION_PROVIDER` で切り替える `get_translation_provider()` を実装
  - `providers/voice.py`：`ElevenLabsVoiceProvider` クラスと、環境変数 `VOICE_PROVIDER` で切り替える `get_voice_provider()` を実装
  - 将来、別のサービス（またはXTTSなどの無料の技術）に切り替えたくなった場合、新しいクラスを追加して分岐を増やすだけで対応できる設計にした
- `app.py` を更新し、DeepL・ElevenLabsを直接呼び出していた箇所を、すべてプロバイダー経由の呼び出しに置き換えた
  - `translate_with_retry()`, `build_language_table()`, `clone_voice_from_sample()`, `speak_with_cloned_voice()` を、それぞれ`_translation_provider`, `_voice_provider`を使うように変更
- 開発サーバー上で、プロバイダー経由でのエンドツーエンドテスト（声のサンプル登録→翻訳→原文・翻訳文それぞれの読み上げ）を実施し、すべて成功
- GitHubにpush済み。実機での動作確認（ElevenLabsのStarterプランへのアップグレード後）はこれから

### ElevenLabs Starterプランへのアップグレード後の不具合修正

- ユーザーがElevenLabsをStarterプラン（月$5）にアップグレードし、ElevenLabsのWebサイト上では声のクローン・読み上げに成功したことを確認
- VoiceBridge側で声のサンプルを登録しようとしたところ、以下のエラーが発生
  - `'message': 'Labels must be serialized dictionary object.'`
- 原因を調査したところ、ElevenLabs公式ライブラリの`voices.ivc.create()`で`labels`引数を省略した場合、
  内部的にPythonの`Ellipsis`（`...`）がそのまま送信されてしまい、API側で「辞書型ではない」とエラーになることが判明
- `providers/voice.py`を修正し、`labels={}`（空の辞書）を明示的に渡すことで回避
- 開発サーバー上でモックテストを行い、`labels`が正しく空の辞書として渡されることを確認
- GitHubにpush済み。実機での最終確認はこれから

### コード全体の総点検と改善

ユーザーから「コードをすべてチェックして不具合をすべて直し、改善できる点をすべて改善してほしい」との依頼を受け、
`app.py`, `db.py`, `providers/`, `templates/`, `static/app.js`, `static/style.css` を一通り精査した。

見つかった問題と対応：

1. **【重大なバグ】履歴が300件を超えると新しい会話が表示されなくなる問題**
   - `db.get_history_for_user()` が「古い順に300件」を取得する実装になっており、
     履歴が300件を超えると、それ以降ずっと古い履歴しか表示されなくなる不具合があった
   - 「直近300件を取得してから古い順に並べ替える」方式に修正
   - 修正の過程で、SQLiteのサブクエリ内で `rowid` を直接参照すると順序が正しく反映されないという
     ハマりどころも発見し、`rowid AS _rowid` として明示的に列に切り出すことで解決した

2. **履歴削除時に音声ファイルが残り続ける問題**
   - 個別削除・一括削除のどちらでも、対応するmp3ファイルがサーバー上に残り続け、
     ディスク容量を圧迫し続ける状態だった
   - `db.delete_history_entry()`, `db.clear_history_for_user()` が削除した音声ファイルのURL一覧を返すように変更し、
     `app.py`側で実際にファイルも削除する `_delete_audio_files()` を追加した

3. **命名の一貫性の改善**
   - プロバイダー設計に変更したにもかかわらず、`db.py`内の関数・列名が `elevenlabs_voice_id` のまま残っていた
   - `voice_provider_id` に改名（列名のマイグレーション処理も追加し、既存データベースでも問題なく移行できるようにした）

4. **セキュリティ・堅牢性の強化**
   - アップロードできるファイルサイズに上限（10MB）を設定し、悪意ある・不具合のあるリクエストでディスクを
     圧迫されるリスクを軽減
   - セッションクッキーに `HttpOnly`, `SameSite=Lax` を設定し、基本的なセキュリティを強化
   - 新規登録時のユーザー名に文字数上限（50文字）のバリデーションを追加
   - アップロードサイズ超過時（413エラー）に、分かりやすいメッセージを返すエラーハンドラーを追加

5. **UXの改善**
   - 翻訳サービスの準備に失敗した場合（APIキー未設定やネットワーク不調など）、
     画面上に分かりやすい警告バナーを表示するようにした（今までは言語選択欄が無言で空になるだけだった）

開発サーバー上で、以下のテストを実施しすべて成功：
- 履歴の順序修正（10件登録し、直近5件を取得して正しい順序で返ることを確認）
- 履歴削除に伴う音声ファイルの実際の削除（ファイルを実際に作成し、削除APIを呼んで消えることを確認）
- 命名変更後の関数の動作
- （アップロードサイズ制限は、Flaskテストクライアント特有の制約により自動テストでの完全な再現はできなかったが、設定自体は正しく反映されていることを確認。実際のブラウザ・本番サーバーでは機能する設計）

GitHubにpush済み。実機での動作確認はこれから

### ElevenLabs音声品質の改善（公式ガイドラインに基づく調整）

- ユーザーから「原文・翻訳文の発音が不自然、ごく自然でネイティブな発音にしてほしい」との指摘
- ElevenLabs公式ドキュメント・ブログを調査し、声のクローン品質を上げるための公式な推奨事項を確認
  - 録音は1〜2分程度が推奨（3分を超えると逆に品質が落ちることもある）。今までの15秒は短すぎた
  - 「デジタル処理の強さ」よりも「録音環境そのものの静かさ・明瞭さ」の方が重要。過度なノイズ除去はかえって不自然さの原因になりうる
  - `similarity_boost`（声の再現度）や`stability`（安定性）などの設定を調整できる
- 上記を踏まえて、以下を改善した
  1. `providers/voice.py`：`text_to_speech.convert()`に`voice_settings`（`stability=0.5`, `similarity_boost=0.85`, `use_speaker_boost=True`）を明示的に指定するようにした
  2. `app.py`：声のサンプル変換時のffmpegフィルタを、以前XTTS向けに調整していた強めの処理（highpass+afftdn+silenceremove）から、最低限の処理（highpassのみ）に軽減した。サンプリングレートも24000Hzから44100Hzに変更
  3. `static/app.js`, `templates/index.html`：録音時間を15秒から60秒（1分）に延長した
- 開発サーバー上で、`VoiceSettings`の正しいフィールド名（`stability`, `similarity_boost`, `style`, `use_speaker_boost`）を実際に確認したうえで実装し、モックテストで正しく渡されることを確認
- GitHubにpush済み。実機での効果確認（声のサンプルの録り直しが必要）はこれから

### 読み上げ速度の調整

- ユーザーから「発音は自然になったが、読み上げスピードが若干速い」とのフィードバック
- `providers/voice.py`の`VoiceSettings`に`speed=0.9`（標準の0.9倍）を追加し、少しゆっくりめに調整
- 開発サーバー上でモックテストを行い、`speed`パラメータが正しく渡されることを確認
- GitHubにpush済み。実機での効果確認はこれから

### 原文・翻訳文で異なる読み上げ速度に調整

- ユーザーから「原文の読み上げは元の速度(1.0)の方が良かった、翻訳文はまだ少し速い」とのフィードバック
- `providers/voice.py`の`speak()`に`speed`引数を追加し、呼び出し側で個別に指定できるようにした
- `app.py`を更新し、原文は標準速度（`speed=1.0`）、翻訳文は少しゆっくりめ（`speed=0.85`）で読み上げるようにした
- 開発サーバー上でモックテストを行い、原文・翻訳文それぞれに正しい速度が渡されることを確認
- GitHubにpush済み。実機での効果確認はこれから

### 原文の音質改善（speed指定の撤廃）+ 言語表示の改善

- ユーザーから「原文読み上げの精度が悪すぎる。2つ前のもの（speedパラメータを指定していなかった状態）が一番良かった」との指摘
  - `providers/voice.py`の`speak()`を、`speed`が指定されない場合はVoiceSettingsに含めない仕様に変更
  - `app.py`側で、原文は速度指定なし（最も自然な音質を優先）、翻訳文は`speed=0.85`のまま維持するようにした
- ユーザーから「言語選択欄を日本語表記＋英語表記の併記にしてほしい」「主要な言語ほど上に来るようにしてほしい」との要望
  - `app.py`に、主要言語の日本語名の対応表（`LANGUAGE_JAPANESE_NAMES`）と、世界的な使用頻度に基づく優先順位リスト（`LANGUAGE_PRIORITY_ORDER`）を追加
  - `build_language_table()`の表示名を「日本語（Japanese）」のような日英併記形式に変更
  - `/`ルートで、優先順位に基づいて言語一覧を並び替えるように変更（アルファベット順から変更）
  - `templates/index.html`のデフォルト選択ロジックを、新しい表示形式に合わせて修正（`name == 'japanese'` → `name.startswith('日本語')`）
  - 不要になった`text-transform: capitalize`をCSSから削除
- 開発サーバー上で、モックデータを使い、表示名の日英併記・主要言語順での並び替えが正しく機能することを確認
- GitHubにpush済み。実機での効果確認はこれから

### 原文の発音精度改善（言語を明示的に指定するモデルへの切り替え）

- ユーザーから「読み上げ速度はそのままで、原文をネイティブな発音にしてほしい」との要望
- ElevenLabsのモデルについて調査
  - 現在使用中の`eleven_multilingual_v2`は`language_code`（発音言語の明示指定）に対応していない（自動判定のみ）
  - `eleven_turbo_v2_5`であれば`language_code`を明示指定でき、さらに`apply_language_text_normalization`という、
    日本語の発音精度を上げる専用オプションもあることが判明
- 翻訳文（現状「完璧」との評価）には手を加えず、**原文の読み上げ時だけ**新しいモデルに切り替える設計にした
  - `providers/voice.py`：`language_code`が指定された場合のみ`eleven_turbo_v2_5`＋`apply_language_text_normalization=True`を使う。指定が無ければ従来通り`eleven_multilingual_v2`を使う
  - `app.py`：原文の読み上げ時に、話す言語のコード（例: "ja"）を`language_code`として渡すようにした
- 開発サーバー上でモックテストを行い、原文と翻訳文でそれぞれ異なるモデル・設定が正しく使い分けられることを確認
- GitHubにpush済み。実機での効果確認はこれから

### 発音間違い（固有名詞）について、現状維持で合意

- 「天下一品」を「てんかいちっぴん」と誤読した件について、ElevenLabsの「発音辞書（Pronunciation Dictionary）」機能を使えば個別に修正できることを説明
- ユーザーの判断により、今回は稀な誤読みとして許容し、発音辞書機能の実装は見送ることで合意
- 全体的な発音品質については「おおむね問題ない」との評価を得られており、原文・翻訳文とも実用レベルに到達したと判断

### UX/UI改善（第一弾）

ユーザーから「UIを含めてUXを最大限向上させたい。仕様をまとめて実装してほしい」との依頼を受け、
以下の8項目からなる改善スペックをまとめ、実装した。

1. **再生ボタンの状態表示**：再生中は▶️→⏸️アイコンに切り替わるようにした（`static/app.js`の`makePlayButton()`を拡張）
2. **ワンタップでコピー**：翻訳文の横にコピーボタンを追加。コピー成功時はチェックマークに一時的に変化する（`makeCopyButton()`を新規追加）
3. **履歴に日時表示**：各会話の下に、控えめな日時表示を追加（`formatTimestamp()`を新規追加。`app.py`のレスポンスにも`timestamp`を含めるように変更）
4. **エラー表示の改善**：エラー発生時、目立つバナー形式（`error-banner`）で表示し、6秒後に自動的に消えるようにした（`showError()`を新規追加）
5. **空の状態の案内**：履歴が0件の時に、使い方のヒントメッセージを表示するようにした（`updateEmptyState()`を新規追加。新規追加時・削除時・初回読み込み時すべてで呼び出すように統一）
6. **新着メッセージへの自動スクロール**：発話後、会話履歴のトップまで自動的にスムーズスクロールするようにした（`scrollToTranscriptTop()`）
7. **録音の進捗を視覚化**：60秒のカウントダウンを、ボタンの背景に重なる形の横長プログレスバーとしても表示するようにした（`templates/index.html`に`record-button__progress`要素を追加、`static/style.css`にスタイル追加）
8. **処理中のローディング表示**：マイクボタンに、処理中は点滅するアニメーション（`processing`クラス）を追加した

開発サーバー上で、Python/JavaScriptの文法チェック、`/api/process`のレスポンスに`timestamp`が正しく含まれることをモックテストで確認し、成功。
GitHubにpush済み。実機での見た目・使用感の確認はこれから

### 会話モード（双方向コミュニケーション対応）の追加

- ユーザーから「単発の翻訳ではなく、相手との双方向のやり取りを、聞き取り→翻訳・読み上げ→聞き取り、と連続して行いたい」との仕様変更の要望
- 「会話モード」という新しいチェックボックスを追加し、有効にすると以下の流れが自動化されるようにした
  1. 読み上げが終わったら（`player`の`ended`イベントを検知）
  2. 自動的に「話す言語」と「翻訳する言語」を入れ替える（`swapLanguages()`という共通関数に切り出し）
  3. 少し間を置いてから、自動的にマイクでの聞き取りを再開する
- これにより、Aさんが話す→翻訳されてBさんの言語で再生→（言語が入れ替わり）Bさんの返事を自動で聞き取る→…という、
  対面での自然な会話のやり取りができるようになった
- 会話モードは既存の「自動終了」「手動終了」モードとも併用可能（会話モードは「読み上げ後にどうするか」を制御し、
  聞き取り自体の終了方法は従来の設定に従う）
- 会話モードを終了したい場合は、チェックを外す、またはマイクボタンを押して聞き取りを中断すればよい
- 開発サーバー上で、JavaScriptの文法チェック、チェックボックスがHTMLに正しく含まれることを確認
- GitHubにpush済み。実機での動作確認はこれから

### 自動言語判別モードの実装（Google Cloud Speech-to-Text連携）

- ユーザーから「言語を手動で入れ替えるのではなく、音声から自動で言語を判別してほしい（一方が話し続ける場合に対応できない）」との要望
- ブラウザの音声認識（Web Speech API）は認識前に言語を指定する必要があり、真の自動判別ができないという技術的制約を説明
- Google Cloud Speech-to-Text（無料枠：月60分、超過後は1分$0.016、上限なしの従量課金）の料金を調査し、ユーザーの合意のもと導入を決定
- ユーザーのGoogle Cloudアカウント作成、プロジェクト作成、Speech-to-Text APIの有効化、APIキーの発行をサポート

**実装内容：**
- `providers/speech.py` を新規作成
  - `GoogleSpeechProvider`：Google Cloud Speech-to-Text（REST API、APIキー認証）を使い、2つの候補言語（`alternativeLanguageCodes`）を渡して自動判別しながら文字起こしする
  - 音声はffmpegでFLAC形式（16kHz、モノラル）に変換してから送信する
  - `get_speech_provider()`：環境変数`SPEECH_PROVIDER`, `GOOGLE_SPEECH_API_KEY`に応じてプロバイダーを選ぶ（未設定時はNoneを返し、ブラウザの音声認識のみを使う）
- `app.py` に `/api/recognize-audio` エンドポイントを追加：録音データと2つの候補言語コードを受け取り、認識結果と判別された言語コードを返す
- `templates/index.html` に「🌐 自動言語判別モード」チェックボックスを追加（音声認識APIが未設定の場合は無効化され、注意書きを表示）
- `static/app.js` の音声認識ロジックを拡張
  - 自動判別モードでは、ブラウザのWeb Speech API（`recognition`）は「発話の終わりを検知するタイマー」としてのみ使用し、実際の文字起こし結果は使わない
  - 同時にMediaRecorderで生の音声を録音し、発話終了時にサーバーの`/api/recognize-audio`にアップロード
  - 判別された言語が「話す言語」「翻訳する言語」のどちらに近いかを比較し、実際の翻訳方向（source/target）を自動的に決定してから`/api/process`に送る
  - 会話モードとの連携：自動判別モードが有効な場合、読み上げ後の言語スワップ処理はスキップする（自動判別が毎回方向を決めるため、スワップ自体が不要になる）
- 開発サーバー上で、`providers/speech.py`単体のテスト（モックによる認識結果・判別言語の確認）、`/api/recognize-audio`エンドポイントのテスト（正常系・プロバイダー未設定時のエラー）、Flaskテンプレートへの反映（チェックボックスの有無、無効化状態、注意書き）を実施し、すべて成功
- GitHubにpush済み。実機での動作確認（Google Cloud APIキーの設定、実際の言語判別精度）はこれから

### UIの本格的リニューアル（プロ品質への引き上げ）

- ユーザーから「UIに素人感が出ているので、プロ品質にしてほしい」との依頼
- 診断：機能追加のたびにチェックボックスを縦に積み重ねてきたことで「設定の羅列」感が出ていたことが主要因と特定
- `templates/index.html` を全面的に再構築
  - 「声のサンプル」と「言語選択」を1つの`setup-card`に統合
  - 4つのチェックボックス（クローン音声、手動モード、会話モード、自動言語判別）を、`<details>`/`<summary>`を使った折りたたみ式の「詳細設定」パネルにまとめ、通常時は隠すようにした
  - 各設定項目に、太字の見出し＋小さな説明文という一貫した構造を持たせた
- `static/style.css` を全面的に書き直し
  - デザイントークン（余白: 4の倍数スケール、角丸: sm/md/full の3段階、影: sm/mdの2段階）を`:root`で明文化し、ファイル全体で一貫して使用するようにした
  - 以前バラバラだったrgba値のハードコードを整理し、統一したトークンを参照する形に変更
  - フォーカス時のアウトライン、`prefers-reduced-motion`への対応などアクセシビリティ面も強化
- 既存の全JavaScriptの要素IDが新しいHTML構造でも変わらず参照できることを確認（機能面は無変更、見た目のみ刷新）
- 開発サーバー上で、CSSの構文チェック、Flaskでの表示確認（詳細設定の存在、setup-cardの存在）、JavaScriptが参照する全19個の要素IDが新HTMLに漏れなく存在することを確認し、成功
- GitHubにpush済み。実機での見た目確認はこれから

### UI追加改善（音声トーン、録音ボタンの控えめ化、詳細設定の常時表示化、ひみつ道具感の強化）

ユーザーから5つの要望を受けた。

1. **読み上げ音声のトーンを明るく**：`providers/voice.py`のVoiceSettingsで`style`を0.0→0.35に、`stability`を0.5→0.4に調整し、抑揚のある明るい話し方になるようにした
2. **録音ボタンを2回目以降は控えめに**：`has_voice_sample`が真の場合、テンプレートで小さなテキストリンク風ボタン（`record-button--subtle`）に切り替え。`app.js`にも`hasVoiceSample`変数と`resetRecordButtonLabel()`関数を追加し、初回録音成功時にもその場でボタンが控えめな見た目に切り替わるようにした（ページ再読み込み不要）
3. **詳細設定を折りたたみパネルから常時表示に戻す**：`<details>`要素をやめ、「会話オプション」という常時表示のカードに変更。あわせてチェックボックスをiOS風のトグルスイッチのデザインに一新した
4. **著作権に配慮しつつ、ひみつ道具感を強化**：キャラクター固有のシンボル（ベルやたけコプターなど）は再現せず、代わりにオリジナルデザインとして、マイクボタンに立体的な「押しボタン」の質感（押下時に沈み込むアニメーション）を追加し、ヘッダー部分に淡い光の演出を加えた
- ユーザーからの指摘で見つかった副次的なバグも修正：`app.js`内で古いクラス名（`voice-setup__status--ok`）が残っていたのを、新しい命名（`status-pill--ok`）に修正
- 開発サーバー上で、Python/JavaScript/CSSの文法チェック、全19個の要素IDの存在確認、声のサンプル未登録・登録済み双方でのテンプレート出力（ボタンの見た目の切り替え、詳細設定が折りたたみで無いこと）を確認し、すべて成功
- GitHubにpush済み。実機での見た目・使用感の確認はこれから

### 追加機能5点セットの実装（お気に入り、ダークモード、クイック言語ペア、チュートリアル、PWA化）

ブレインストーミングで提示した8つの追加改善案から、ユーザーが選んだ5つを実装した。

1. **よく使うフレーズの保存・ワンタップ送信**
   - `db.py`：`favorites`テーブルを新規追加（利用者ごとに分離）。`add_favorite`, `get_favorites_for_user`, `delete_favorite`関数を実装
   - `app.py`：`/api/favorites`（GET/POST）、`/api/favorites/<id>`（DELETE）エンドポイントを追加
   - `static/app.js`：各会話の原文吹き出しに★ボタンを追加してお気に入り登録できるようにし、テキスト入力欄の下に登録済みフレーズをチップ形式で表示。タップで即座に送信、×で削除できる

2. **ダークモード**
   - `static/style.css`：`[data-theme="dark"]`属性で切り替わる、ダーク版のカラートークン一式を追加
   - `static/app.js`：ヘッダーの太陽/月アイコンボタンで切り替え。`localStorage`に保存し、次回訪問時も保持。初回はOSの設定（`prefers-color-scheme`）に従う

3. **言語ペアのクイック切り替え**
   - `static/app.js`：よく使う言語の組み合わせを`localStorage`に保存し、チップとして言語選択カードの下に表示。タップで即座に両方の言語を切り替えられる（最大6件、超過時は古いものから削除）

4. **初回起動時の使い方ガイド（チュートリアル）**
   - `templates/index.html`：4ステップのチュートリアルモーダルを追加
   - `static/app.js`：初回訪問時（`localStorage`で判定）に自動表示。ヘッダーの「？」ボタンからいつでも再表示できる

5. **PWA化（ホーム画面に追加してアプリのように使える）**
   - PIL（Pillow）を使い、ひみつ道具バッジのデザインを踏襲したアプリアイコン（192px, 512px）を生成。日本語フォント（Noto Sans CJK）を使うことで文字化けを解消
   - `static/manifest.json`、`static/sw.js`（最小限のサービスワーカー）を新規作成
   - `app.py`：`/sw.js`をルートスコープで配信するルートを追加し、`before_request`の許可パスにも追加
   - `templates/index.html`：manifestへのリンク、テーマカラー、apple-touch-iconを追加

開発サーバー上で、以下を実施しすべて成功：
- `db.py`のお気に入り機能単体テスト（追加・一覧取得・削除）
- `/api/favorites`関連エンドポイントのテスト（正常系・空文字列の拒否）
- `/sw.js`・`/static/manifest.json`が未ログインでも取得できることの確認
- Python/JavaScript/CSSの文法チェック
- 新しく追加した12個の要素IDを含む、全31個の要素IDがHTMLに漏れなく存在することの確認

GitHubにpush済み。実機での動作確認（アイコン生成の見た目、各機能の実際の使用感）はこれから

### 緊急バグ修正：チュートリアルモーダルによるアプリ全体のフリーズ

- ユーザーから「アプリ上のどこを押しても画面が動かない」との報告
- 原因を特定：`.tutorial-overlay`のCSSに`display: grid`を指定していたため、
  HTML側の`hidden`属性（本来は要素を隠すためのもの）が上書きされてしまい、
  意図せず**常にモーダルオーバーレイが画面全体に表示され、すべてのクリックを奪ってしまっていた**
- 修正：`hidden`属性に頼る方式をやめ、`.is-visible`というCSSクラスの付け外しで表示・非表示を制御する方式に変更
  - `static/style.css`：`.tutorial-overlay`のデフォルトを`display: none`にし、`.tutorial-overlay.is-visible`の時だけ`display: grid`にするよう変更
  - `static/app.js`：`tutorialOverlay.hidden = true/false`だった箇所を、`classList.add/remove("is-visible")`に変更
  - `templates/index.html`：不要になった`hidden`属性を削除
- 開発サーバー上で、CSSの構文チェック、重複ルールが無いことを確認し、GitHubにpush済み
- 実機での動作確認はこれから

### ハンズフリーモードの追加

- ユーザーから「運転中や作業中に画面操作しなくていいよう、アプリを開いたら音声聞き取り常時オンになるモードがほしい」との要望
- `templates/index.html` に「🚗 ハンズフリーモード」のトグルスイッチを追加
- `static/app.js` を更新
  - `recognition.onend`：何も聞き取れなかった場合（無音など）も、ハンズフリーモードなら自動的に聞き取りを再開するようにした（今までの会話モードは「読み上げ後」のみの継続だったため、無音時にも対応させる必要があった）
  - `player`の`ended`イベント：会話モードとハンズフリーモードの両方に対応するよう統合。会話モードの時だけ言語を入れ替え、ハンズフリーモード単独では同じ方向で聞き取りを継続する
  - 音声再生に失敗した場合、エラーが発生した場合（`showError`関数内）も、ハンズフリーモードなら自動的に聞き取りを再開するようにした
  - `localStorage`に設定を保存し、次回アプリを開いた時も自動的にハンズフリーモードがオンになり、聞き取りが自動開始されるようにした
- 開発サーバー上でJavaScriptの文法チェック、要素IDの存在確認、Flaskでのテンプレート出力確認を実施し、すべて成功
- GitHubにpush済み。実機での動作確認はこれから

【正直な注意点】ブラウザベースの仕組みのため、スマホの画面をロックしたり、別のアプリに切り替えたりすると、
マイクの聞き取りが停止する可能性が高い（ブラウザタブがバックグラウンドに回ると、多くのスマホでマイクアクセスが制限されるため）。
本当の意味でバックグラウンドでも動き続ける仕組みには、専用のネイティブアプリ化が必要になる。この点はユーザーへの実機確認時に説明する。

### 読み上げの声色（トーン）選択機能の追加

- ユーザーから「翻訳文読み上げの声色を選択できるようにしてほしい。友達との明るい声色、ビジネスの真面目な声色など」との要望
- `providers/voice.py`：`VOICE_TONE_PRESETS`（standard/friendly/business）を定義し、ElevenLabsの`stability`・`style`パラメータを声色ごとに調整するようにした
  - friendly（明るい）：stability低め・style高め（抑揚豊かでテンション高く聞こえる）
  - business（落ち着いた）：stability高め・style低め（安定していて、まじめな話し方に聞こえる）
- `app.py`：edge-tts（自然な声、クローン未使用時）にも同様のトーン調整を適用。`EDGE_TTS_TONE_PRESETS`で話速（rate）とピッチ（pitch）を声色ごとに変えるようにした
  - これにより、クローン音声・自然な声のどちらを使っていても、声色の選択が反映されるようにした
- `/api/process`に`voice_tone`パラメータを追加し、原文・翻訳文どちらの読み上げにも同じトーンを適用するようにした
- `templates/index.html`：「会話オプション」カードの上部に、3択のセグメントボタン（標準／😄 明るい／💼 落ち着いた）を追加
- `static/app.js`：選択したトーンを`localStorage`に保存し、次回訪問時も保持されるようにした
- 開発サーバー上で、声色ごとに異なる設定値が正しく適用されること（モックテスト）、`/api/process`から実際に声のクローンプロバイダーまで正しくトーンが伝播すること、不正な値が"standard"にフォールバックすることを確認し、すべて成功
- GitHubにpush済み。実機での聞き比べ確認はこれから

### 声のトーン変化を大幅に強化

- ユーザーから「もっと大幅に声色を変えてほしい」との追加要望
- `stability`/`style`パラメータの調整だけでは変化が控えめだったため、**ピッチ（声の高さ）を変える後処理**を追加
  - `providers/voice.py`に`_shift_pitch()`関数を新規実装。ffmpegの`asetrate`（再生速度ごと変える）→`atempo`（速度だけ元に戻す）という定番の手法で、テンポ（長さ）は変えずに音の高さだけを変える
  - 実装前に、単体で周波数解析（FFT）を使い、指定した倍率通りに周波数が変化することを検証してから組み込んだ（440Hz→1.08倍で475Hz、0.90倍で396Hzなど、期待通りの結果を確認）
- 声のトーンのプリセットを、より極端な値に調整
  - friendly（明るい）：stability=0.15、style=0.85、speed=1.08、ピッチ×1.10
  - business（落ち着いた）：stability=0.80、style=0.0、speed=0.90、ピッチ×0.90
- edge-tts（自然な声）側のプリセットも、話速・ピッチの変化幅を拡大
- テスト中に、モックの使い回しによる不具合（2回目以降の呼び出しでイテレータが空になる）を発見し、テストコード側を修正（`side_effect`を使う方式に変更）。実装自体のバグではないことを確認できた
- 修正後、実際にElevenLabsのレスポンスを模したmp3データを使い、周波数解析でトーンごとにピッチが正しく変化していることを最終確認（標準440Hz→明るい484Hz、落ち着いた396Hz）
- GitHubにpush済み。実機での聞き比べ確認はこれから

【正直な注意点】原文の音声にも、標準以外のトーンを選んだ場合は速度パラメータが明示的に設定されるようになった。
以前「原文は速度指定なしが一番自然」と判明していたため、非標準トーン選択時は原文の音質がわずかに影響を受ける可能性がある。
これは「大幅な変化」を実現するためのトレードオフとして許容している。

---

## 2026-09-11（新しいチャットに引き継ぎ）

前の会話が画像枚数の上限に達したため、CURRENT_STATE.mdとDEVELOPMENT_LOG.mdをGitHub経由で読み込み、このチャットに引き継いだ。

### 環境変数エラーの解消（ユーザーのPC環境の問題）

- ユーザーのPCで「翻訳サービスの準備ができていません」（対応言語数：0 言語）というエラーが継続していた
- 調査の結果、以下2つの環境的な問題が原因と判明
  1. Windowsのユーザー環境変数に `DEEPL_API_KEY` が正しく登録されていなかった（`ELEVENLABS_API_KEY` は登録済みだった）
  2. プロジェクトフォルダが、想定していた `Desktop\VoiceBridge`（コミット0件の空フォルダ）ではなく、実際には `Downloads\webapp`（コミット履歴のある本物のフォルダ）にあることが判明。混乱の元になっていた過去の重複クローンを特定した
- `DEEPL_API_KEY` をシステム環境変数に正しく登録し直したことで解消

### 声のトーン機能の音質改善（2件）

ユーザーからのフィードバック「9/10 00:11の読み上げが一番自然だった。精度が落ちている」を受けて調査したところ、
実際にはコード・トーン設定（😄 明るい）とも当時と全く同じであることが判明。設定を「戻す」余地は無いため、
根本原因を2つ特定して修正した。

1. **ピッチ変更処理の再エンコード品質を改善**（`providers/voice.py`）
   - 「明るい」「落ち着いた」トーンでは、声の高さを変えるためにffmpegで音声を一度展開→加工→再度mp3に圧縮し直す処理（`_shift_pitch`）を行っている
   - このとき出力のビットレートを明示的に指定していなかったため、生成のたびに音質が不安定になっていた可能性があった
   - `-b:a 192k -ar 44100` を明示的に指定し、ElevenLabs出力（128kbps）より低い品質で再圧縮されないようにした
   - （※このユーザーPC側の修正は、ユーザー自身がメモ帳で直接編集してpushする形で先に反映された）

2. **【重大なバグ修正】「明るい」トーンで翻訳文読み上げ時に雑音が発生する問題**（`app.py`）
   - ユーザーからの報告：「標準・落ち着いたは問題ないが、明るいは翻訳文の読み上げのみ雑音が出る」
   - 原因を特定：翻訳文の読み上げには、トーン機能とは別に元からあった「聞き取りやすくするため少しゆっくり（speed=0.85）」という速度指定が常に適用されていた
     - 「明るい」トーンは本来 speed=1.08（かなり速い）+ stability=0.15（意図的にかなり不安定・表現豊か）という設定だが、
       0.85への強制上書きにより、トーンが意図した速度と正反対の方向に大きく矛盾してしまい、不安定性の高い設定と組み合わさって音声生成が乱れ、雑音が発生していたと判明
     - 「落ち着いた」はspeed=0.90で、0.85との差が小さかったため、同じ仕組みの影響を受けにくく問題が出ていなかった
   - 修正：標準トーンの時だけ、この0.85への速度上書きを適用するように変更。「明るい」「落ち着いた」では、トーン自身が意図した速度をそのまま使うようにした

### 表記変更

- ユーザーの希望により、声のトーンの表示名を「😄 明るい」→「😄 高」、「💼 落ち着いた」→「💼 低」に変更（`templates/index.html`）
- 内部のコード上の識別子（`friendly`, `business`）はそのまま変更していない

### 開発フローの変更：Claude側からの直接push運用に移行

- ユーザーから「手作業が面倒なので、なるべく自動でやってほしい」との要望を受け、Claude（このセッション）がGitHubリポジトリを直接 `git clone` し、
  コードの調査・修正・コミット・pushまで一貫して行えることを確認した（従来はユーザーのPC上で手動編集・pushしてもらう運用だった）
- ユーザーにFine-grained Personal Access Token（Contents: Read and write権限）を再発行してもらい、それを使ってClaudeが直接pushする運用に切り替えた
- **今後の会話では、コード修正が必要な場合はClaudeが直接GitHubからclone・修正・pushまで行い、ユーザーの手作業（ダウンロード・メモ帳編集・git操作）を最小限にする**

開発サーバー上で、修正箇所の構文チェック（`py_compile`）、ffmpegの実際の再エンコードテスト（ビットレートが192kbpsになることを確認）を実施し、成功。
GitHubにpush済み。実機での動作確認（環境変数の反映、「高」トーンでの雑音解消、表記変更の見た目）はこれから
